import "./App.css";
import AppRouter from "./AppRouter";

function App() {
  return (
    // <ConfigProvider locale={esES}>
    // {fullPageLoading && <FullPageLoader />}
    <AppRouter></AppRouter>
    // <Dialogs />
    // </ConfigProvider>
  );
}

export default App;
